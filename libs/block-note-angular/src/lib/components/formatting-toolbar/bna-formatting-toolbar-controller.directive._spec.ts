import { BnaFormattingToolbarControllerDirective } from './bna-formatting-toolbar-controller.directive';

describe('BnaFormattingToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new BnaFormattingToolbarControllerDirective();
    expect(directive).toBeTruthy();
  });
});
