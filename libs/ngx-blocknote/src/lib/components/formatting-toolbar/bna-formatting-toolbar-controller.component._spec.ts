import { BnaFormattingToolbarControllerComponent } from './bna-formatting-toolbar-controller.component';

describe('BnaFormattingToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new BnaFormattingToolbarControllerComponent();
    expect(directive).toBeTruthy();
  });
});
