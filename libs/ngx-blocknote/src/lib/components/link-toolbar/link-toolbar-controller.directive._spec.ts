import { BnaLinkToolbarControllerComponent } from './link-toolbar-controller.component';

describe('BnaLinkToolbarControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new BnaLinkToolbarControllerComponent();
    expect(directive).toBeTruthy();
  });
});
